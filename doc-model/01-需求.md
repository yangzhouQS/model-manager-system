

存储问题：

租户、运行时



模型：

模型关系结构：

> 模型结构
>

+ 单表模式
+ 单表扩展表模式
+ 一主多从
+ 主从主从模式



> 模型选择范围
>

+ 单库组合
+ 跨库组合（不支持支撑和SaaS跨库）
+ 自定义数据来源



模型属性集：

+ 属性子集：
    - 属性名：
        * 数据库字段名称，小驼峰格式
        * 自定义命名
    - 类型
        * varchar：string
        * bigint：number
    - 默认值
    - 显示名称



模型归属：

+ 模型分组
+ 模型->模块关联（可选）



模型变更:

+ 上下游依赖关系
    - 模型发生变化对下游影响，
    - 强耦合，上游强制控制下游引用。表单字段名称修改，影响上游名称。
    - 低耦合，下游不受上游模型影响。







强继承：

新增

修改字段

删除字段

字段冲突
